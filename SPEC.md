# VimGym — Vim 키바인딩 학습 도구

> "실전 코드 편집 퍼즐로 Vim을 몸에 익히는 TUI 트레이너"

## 1. 핵심 컨셉

터미널에서 **Before → After 코드 변환 퍼즐**을 풀면서 Vim 명령어를 체득하는 도구.

- Neovim을 내장해 **실제 Vim과 동일한 편집 경험**
- 최소 키스트로크(Vim Golf) 스코어링
- 설치하고, 배우고, 졸업하는 순수 로컬 도구

## 2. 사용자 흐름

```
Track 1: Foundations ─→ Track 2: Editing ─→ Track 3: Power Moves ─→ Track 4: Vim Golf ─→ 졸업 🎓
     (Lv 1~5)              (Lv 6~10)            (Lv 11~15)             (Lv 16~30)
```

각 레벨: **연습 퍼즐 5~7개** → 전부 ⭐ 이상 클리어 시 다음 레벨 해금

## 3. 기술 스택

| 구성 | 선택 | 이유 |
|------|------|------|
| 언어 | **Go** | 크로스 플랫폼 단일 바이너리 |
| TUI | **Bubble Tea + Lip Gloss** | 모던 TUI 최고 수준 |
| Vim 엔진 | **Neovim embed** (`--embed`) | 실제 Vim 엔진, 100% 정확 |
| Neovim 연동 | **neovim/go-client** | msgpack-rpc 클라이언트 |
| 로컬 저장 | **`~/.vimgym/`** (JSON) | 진행 상태 |
| 퍼즐 임베딩 | **`embed.FS`** | 빌드 시 퍼즐 JSON 내장 |

### 아키텍처

```
┌─ VimGym TUI (Go + Bubble Tea) ──────────────────┐
│                                                  │
│  ┌─ Neovim (--embed) ────────┐                   │
│  │  실제 Vim 엔진             │                   │
│  │  msgpack-rpc 통신          │                   │
│  └───────────────────────────┘                   │
│                                                  │
│  ┌─ TUI Layer ───────────────┐                   │
│  │  퍼즐 표시 (Goal)          │                   │
│  │  Neovim 버퍼 렌더링        │                   │
│  │  키스트로크 카운터          │                   │
│  └───────────────────────────┘                   │
│                                                  │
│  로컬: ~/.vimgym/progress.json                   │
└──────────────────────────────────────────────────┘
```

### Neovim Embed 동작 방식

1. `nvim --embed` 프로세스를 백그라운드 생성
2. Go 앱이 msgpack-rpc로 연결
3. 퍼즐 시작 → 버퍼에 `before` 텍스트 로드 + 커서 설정
4. 키 입력 → Neovim 전달 → 버퍼 변경
5. 버퍼 텍스트가 `after`와 일치 → 클리어

### 사전 요구

```bash
# nvim 설치 필요
nvim --version
```

## 4. 핵심 기능

### 4.1 퍼즐 시스템

```
┌─ VimGym ─────────────────────────────────────────┐
│                                                  │
│  Level 12: Object Surgery                        │
│  Category: Text Objects  |  Par: 9 keystrokes    │
│                                                  │
│  ┌─ GOAL ──────────────────────────────────────┐ │
│  │ const name = "Goodbye";                     │ │
│  └─────────────────────────────────────────────┘ │
│                                                  │
│  ┌─ EDITOR (Neovim) ──────────────────────────┐ │
│  │ const name = "Hello, World!";               │ │
│  │                ^ cursor                     │ │
│  └─────────────────────────────────────────────┘ │
│                                                  │
│  Keystrokes: 0  |  Mode: NORMAL                  │
│                                                  │
│  Ctrl+H Hint  Ctrl+O Solution  Ctrl+R Reset  Ctrl+Q Quit │
└──────────────────────────────────────────────────┘
```

**스코어링:**
| 등급 | 조건 |
|------|------|
| ⭐⭐⭐ | par 이하 |
| ⭐⭐ | par × 1.5 이하 |
| ⭐ | 클리어 |

클리어 후 최적해를 텍스트로 표시 (예: `ci"Goodbye<Esc>` — 9 keystrokes).
문제를 풀지 못했을 때도 `Ctrl+O`로 정답(Optimal Solution) 표시를 토글 가능.

### 4.2 학습 트랙

**Track 1: Foundations (기본 이동)**
| Lv | 명령어 | 주제 |
|----|--------|------|
| 1 | `hjkl` | 기본 이동 |
| 2 | `w`, `b`, `e` | 단어 이동 |
| 3 | `0`, `$`, `^` | 줄 이동 |
| 4 | `gg`, `G`, `{`, `}` | 블록/파일 이동 |
| 5 | `f`, `t`, `F`, `T` | 문자 검색 이동 |

**Track 2: Editing (편집)**
| Lv | 명령어 | 주제 |
|----|--------|------|
| 6 | `i`, `a`, `o`, `O` | 삽입 |
| 7 | `x`, `dd`, `D` | 삭제 기본 |
| 8 | `dw`, `d$`, `d0` | 모션 + 삭제 |
| 9 | `cw`, `cc`, `C` | 변경 |
| 10 | `yy`, `yw`, `p`, `P` | 복사/붙여넣기 |

**Track 3: Power Moves (고급)**
| Lv | 명령어 | 주제 |
|----|--------|------|
| 11 | `ci"`, `ca(`, `di{` | 텍스트 오브젝트 |
| 12 | `vi"`, `vap`, `viw` | 비주얼 + 텍스트 오브젝트 |
| 13 | `/`, `?`, `n`, `N`, `*` | 검색 |
| 14 | `:s`, `:%s` | 치환 |
| 15 | `.`, `q`, `@` | 반복 & 매크로 |

**Track 4: Vim Golf (실전)**
| Lv | 주제 |
|----|------|
| 16 | combo-basics |
| 17 | text-object-combo |
| 18 | yank-combo |
| 19 | marks-jumps |
| 20 | regex-substitute |
| 21 | visual-block |
| 22 | indent-format |
| 23 | advanced-macros |
| 24 | multi-line |
| 25 | refactor |
| 26 | advanced-combo |
| 27 | speed-edit |
| 28 | transform |
| 29 | expert |
| 30 | graduation |

## 5. 퍼즐 데이터 포맷

```json
{
  "id": "text-objects-01",
  "title": "String Surgery",
  "track": 3,
  "level": 11,
  "category": "text-objects",
  "difficulty": 2,
  "before": {
    "text": "const name = \"Hello, World!\";",
    "cursor": { "row": 0, "col": 15 }
  },
  "after": {
    "text": "const name = \"Goodbye\";"
  },
  "par": 9,
  "hint": "Try using ci\" to change inside quotes",
  "optimalSolution": "ci\"Goodbye<Esc>",
  "solutionExplanation": "ci\" changes inside quotes, type Goodbye, Esc to return to normal",
  "tags": ["ci\"", "text-object", "change"]
}
```

## 6. 프로젝트 구조

```
vimgym/
├── cmd/
│   ├── vimgym/
│   │   └── main.go              # 앱 엔트리포인트
│   └── puzzlecheck/
│       └── main.go              # 퍼즐 데이터 검증 도구
├── internal/
│   ├── tui/
│   │   ├── app.go               # Bubble Tea 메인 모델
│   │   ├── puzzle_view.go       # 퍼즐 화면 + 키 입력 처리
│   │   ├── track_view.go        # 트랙/레벨/퍼즐 선택
│   │   ├── progress_view.go     # 전체 진행 현황 표시
│   │   └── styles.go            # Lip Gloss 스타일
│   ├── nvim/
│   │   ├── client.go            # Neovim embed 관리
│   │   └── buffer.go            # 버퍼 조작/퍼즐 로드
│   ├── puzzle/
│   │   ├── types.go             # 퍼즐 데이터 타입 정의
│   │   ├── loader.go            # 퍼즐 JSON 로드 (embed.FS)
│   │   ├── loader_test.go       # validator/scorer 테스트
│   │   ├── validator.go         # 클리어 판정
│   │   └── scorer.go            # 스코어 계산
│   └── progress/
│       └── store.go             # 로컬 진행 저장
├── puzzles/
│   ├── embed.go                 # go:embed FS
│   ├── track1_foundations.json   # 29 퍼즐 (Lv 1~5)
│   ├── track2_editing.json      # 28 퍼즐 (Lv 6~10)
│   ├── track3_power.json        # 29 퍼즐 (Lv 11~15)
│   └── track4_golf.json         # 76 퍼즐 (Lv 16~30)
├── go.mod
└── go.sum
```

## 7. 로드맵

### v0.1 — MVP (완료)
- [x] Go + Bubble Tea 셋업
- [x] Neovim embed 연동
- [x] 퍼즐 화면 (Goal + Neovim 에디터)
- [x] 키스트로크 카운팅 + 클리어 판정 + 스코어링
- [x] 트랙/레벨 선택 + 해금
- [x] 로컬 진행 저장
- [x] Track 1~4 퍼즐 162개 (레벨당 5~7개 × 30레벨)
- [x] 클리어 후 최적해 텍스트 표시
- [x] 퍼즐 데이터 검증 도구 (`cmd/puzzlecheck/`)

### Future — 검토 후 결정
- goreleaser + brew 배포
- 일일 챌린지 + 리더보드 (사용자 수요 확인 후)
- 커뮤니티 퍼즐 제작/공유

## 8. 차별화 요약

| 기존 도구 | VimGym |
|-----------|--------|
| 이동 명령 위주 게임 | **실제 코드** 편집 퍼즐 (전 명령어) |
| 브라우저 Vim 에뮬레이션 | **Neovim embed** — 100% 정확 |
| 클리어/실패만 판정 | **par 대비 스코어링** (⭐ 시스템) |
| 구식 UI | **모던 TUI** (Bubble Tea) |
| 서버 필요 | **순수 로컬**, 서버 의존성 없음 |

---

*Created: 2026-02-14*
*Updated: 2026-02-15*
